<p class="text-green-600"><%= notice %></p>
<% content_for :title, "Editing flight" %>

<h1 class="text-2xl font-bold mb-6">Editing flight</h1>

<%= render "form", flight: @flight %>

<div class="mt-6 flex gap-4">
  <%= link_to "Show this flight", @flight,
              class: "text-blue-600 hover:text-blue-900 underline" %>
  <%= link_to "Back to flights", flights_path,
              class: "text-gray-600 hover:text-gray-900 underline" %>
</div>
