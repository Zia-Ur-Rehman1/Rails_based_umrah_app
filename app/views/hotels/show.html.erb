<p style="color: green"><%= notice %></p>
<%# Turbo Frame for the form (appears above table) %>
<turbo-frame id="hotel_form" class="block mb-8">
  <%# Form will appear here when "New Hotel" or "Edit" is clicked %>
</turbo-frame>
<%= render @hotel %>
